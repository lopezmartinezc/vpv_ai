[project]
name = "vpv-ops"
version = "0.1.0"
description = "TUI for VPV production database operations"
requires-python = ">=3.12"
dependencies = [
    "textual>=1.0.0",
]

[project.scripts]
vpv-ops = "vpv_ops.app:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
